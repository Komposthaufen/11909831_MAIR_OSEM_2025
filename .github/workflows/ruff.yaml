name: Ruff

on: 
  [push, pull_request]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Ruff Linter with Auto-fix
        uses: chartboost/ruff-action@v1
        with:
          args: --fix  # This enables auto-fixing with ruff
          
